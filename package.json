{
  "name": "orthodontia",
  "version": "0.0.1",
  "description": "Change braces to the way you like them.",
  "main": " ",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify src/orthodontia.js src/background.js src/options.js -p [ factor-bundle -o dist/orthodontia.js -o dist/background.js -o dist/options.js ] > NUL && copyfiles -f src/options.html dist",
    "watch": "watchify -v --debug src/orthodontia.js src/background.js src/options.js -p [ factor-bundle -o dist/orthodontia.js -o dist/background.js -o dist/options.js ] -o dummy.js",
    "watch:windows": "start cmd.exe /C npm run watch",
    "prewatch": "echo \"Watch Limitation: Changes to anything besides js files will not automatically be copied to dist/\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/BenjaminBrandtner/Orthodontia.git"
  },
  "keywords": [
    "chrome",
    "plugin",
    "braces"
  ],
  "author": "Benjamin Brandtner",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/BenjaminBrandtner/Orthodontia/issues"
  },
  "homepage": "https://github.com/BenjaminBrandtner/Orthodontia#readme",
  "dependencies": {},
  "devDependencies": {
    "browserify": "^16.5.0",
    "copyfiles": "^2.1.1",
    "factor-bundle": "^2.5.0",
    "watchify": "^3.11.1"
  }
}
